# Tasks

## 1. Create README Banner

- [x] 1.1 Create `README.md` at the repository root with the banner section defined by the `readme-banner` spec, and verify the file exists at the repository root.
- [x] 1.2 Verify the banner names the project via an H1 heading and includes a one-line tagline, and verify the banner is the first non-blank content of the file by inspecting the file contents.
