# Tasks

## 1. Scaffold NOTES.md at the repository root

- [ ] 1.1 Create a new file `NOTES.md` at the repository root with the
  initial scaffold: a level-1 heading that contains the word "Notes", a
  `## Purpose` section whose body explains the file's role relative to
  `README.md`, and at least one reserved level-2 heading
  (`## Conventions`, `## Status`, or `## Open Questions`) whose body
  is empty or contains a single placeholder sentence.

- [ ] 1.2 Verify the scaffold against the `project-notes` capability
  requirements in `specs/project-notes/spec.md`: confirm the file is
  tracked, the first non-blank line is a level-1 "Notes" heading, and
  the `## Purpose` plus at least one reserved heading are present.

## 2. Stage and commit the change

- [ ] 2.1 `git add NOTES.md` and `git add openspec/changes/e2e-loop-scope-g1-notes-basics-02a5fb74` so
  both the implementation file and this change's spec artifacts are
  recorded in the same commit.

- [ ] 2.2 Commit with a message that names this Work Package
  (e.g. `notes-basics: scaffold NOTES.md`). Do NOT run
  `openspec archive` or move this change into `openspec/archive/`; that
  is a later Work Package's responsibility.

